# Agent Efficiency Policy

この文書は、AI / coding agent を使った開発で、品質・安全性を維持しながら不要な再調査、再推論、テスト反復、Codexクレジット消費を抑えるための正式ルールです。

## 優先順位

この文書は、**調査範囲、開始時の文書読み込み、REUSE_MAP横断調査、テスト範囲、反復上限、Codexへの引き継ぎ**について、既存文書の広い表現を具体化・制限します。

既存の `AI_OPERATING_MANUAL.md`、`AI_STARTUP.md`、`DEVELOPMENT_RULES.md` 等に「毎回すべて確認」「類似実装を広く調査」「実装後にテスト」と読める表現があっても、この文書のティア別上限を優先します。

ただし、秘密情報保護、正式ソース確認、Git状態確認、実運用データ保護、本番反映制限、ユーザーが明示したフェーズ境界などの安全ルールは弱めません。

## 基本原則

- 安全性を「工程数」ではなく、**変更リスクに比例した検証**で確保する。
- 一度確立した事実を、コードや前提が変わっていないのに再導出しない。
- 小変更のためにリポジトリ全体・管理文書全体を読み直さない。
- Codexはローカル・Windows実機でしか得られない価値へ優先して使う。
- フルスイートは原則としてCIへ寄せ、Codexの修正ループ内で繰り返さない。
- テスト成功ログを大量にコンテキストへ取り込まない。
- 自律性は維持するが、調査範囲・テスト範囲・反復回数には上限を置く。

# 1. 変更ティア

作業開始時に1行で `T0 / T1 / T2 / T3` のいずれかを宣言し、影響範囲を短く書く。

## T0 機械的

コメント、docstring、ログ文字列、表示文言、整形、明らかに参照されないdead code、挙動を変えない軽微な文書変更。

## T1 局所

1関数または1コンポーネント内部の変更で、公開シグネチャ、戻り値形式、共有状態、設定、スキーマ、IO形式、外部契約を変えないもの。

## T2 連結・業務影響あり

次を含む変更。

- 公開シグネチャ、戻り値形式、共有ユーティリティ
- 設定、CLI、build / deploy / updateスクリプト
- 金額、売上、価格、在庫数、帳票、印刷
- CSV / PMS / Sheets / DB の読み書きや形式
- 共有フォルダ、HDD、外部ファイル同期
- データ移行、保存形式、永続化
- 複数モジュール間の契約変更

## T3 構造的

次を含む変更。

- フレームワーク・ランタイム移行
- major依存更新
- repo横断変更
- CI基盤・既定ブランチ・リリース方式の変更
- 大規模アーキテクチャ変更
- 本番移行方式そのものの変更

作業中に上位ティアだと判明した場合は、ティアと影響範囲を更新してから続行する。T2以上へ上がり、当初依頼の安全条件や運用を実質的に変える場合は既存のスコープ変更ルールを適用する。

# 2. 開始時の読み込み予算

## 常に確認する最小セット

- 対象リポジトリ / ブランチ / HEAD SHA / dirty tree
- この `AGENT_EFFICIENCY_POLICY.md`
- 対象プロジェクトのREADMEまたは対象 `projects/*.md` のうち、今回の変更に直接必要なもの
- 引き継ぎに `【確立した事実】` がある場合はそれ

## T0 / T1

- 管理文書のフル開始チェーンを機械的に読まない。
- touched file と直接の呼び出し元1ホップを上限とする。
- `PROJECT_STATUS.md`、`VERSION_MATRIX.md`、`SYSTEM_OVERVIEW.md`、`docs/decisions.md`、`LESSONS_LEARNED.md` 等は、今回の変更に直接関係するときだけ読む。
- 「文脈把握のため」にrepo全体を広くgrep/readしない。

## T2

- `DEVELOPMENT_RULES.md` と対象プロジェクト文書の関連箇所を読む。
- 変更する契約・形式のproducer / consumer、読み書き両側を確認する。
- 金額・在庫・印刷・DB・共有フォルダ等は、関連する安全ルール・設計判断を追加で読む。
- 管理対象全プロジェクトの状態を一括で読み込む必要はない。

## T3

- `AI_STARTUP.md` のフル開始チェーンを適用する。
- repo横断・既存資産・設計判断・移行経路を広く確認する。

## 再読込禁止

同一セッション / 同一ブランチで、すでに読んだ開始文書を回答ごとに再ロードしない。再読込するのは、その文書・HEAD・前提が変わった場合か、今回のスコープで新たに必要になった場合だけとする。

# 3. 既存資産・REUSE_MAPの発火条件

`REUSE_MAP.md` や他repoの類似実装を横断調査するのは、原則として次の場合だけ。

- 新しい機構・方式を導入する
- 既存方式を置き換える
- T3のrepo横断・構造変更
- 対象repo内に既存方式がなく、再利用候補を探す必要がある

既存機構のバグ修正、表示調整、局所的な条件変更では、REUSE_MAP横断調査を自動発火させない。

# 4. テスト戦略

目的は「テストを減らす」ことではなく、**失敗を見つける能力を保ったまま、同じ安全性をより小さい検証集合で得ること**。

## T0

- 構文 / import確認、または当該ファイルの最小テストのみ。
- フルスイート禁止。

## T1

- targeted testのみ。
- 例: `pytest -k <対象> -q --tb=short -p no:cacheprovider`
- フルスイート禁止。

## T2

- targeted test + blast-radiusで名指ししたregression testを実行する。
- **regression = 全テストではない。** 影響が届くモジュール群だけを対象にする。
- repoのCIがフルスイートをカバーしている場合、Codexはローカルでフルスイートを回さない。push後のCIを正式なフル回帰ゲートとする。
- CIがフルスイートをカバーしていない場合でも、修正ループのたびにフルスイートを回さない。金額・在庫・印刷・DB等の高リスク変更で必要なときだけ、最終チェックポイントで**最大1回**ローカルfullを許可する。

## T3

- フルスイート、必要なdry-run、実環境確認を行う。
- CIで代替できるものはCIを利用し、Codexローカルで同じfullを重複させない。

## 共通

- 失敗1件の診断にフルスイートを使わない。対象テストだけを `-x -q --tb=short` で回す。
- green後にコード・設定・依存が変わっていないなら、安心のためだけに同じテストを再実行しない。
- 成功ログはサマリ1行へ圧縮する。コンテキストへ残すのはFAILED行、短いtraceback、最終サマリを基本とする。
- 253件、392件等のpass詳細をそのまま会話コンテキストへ貼らない。

# 5. 反復上限

- 同じ原因仮説に基づく `修正 → targeted test` は、原則2回まで。
- 2回の修正でgreenにならない場合、同じループを続けない。
- その時点で、`根本原因の仮説 / 試した差分 / 短い失敗ログ / 次の候補` を整理して再設計またはユーザー報告へ移る。
- 上限到達前は、依頼範囲内の安全な調査・設計・実装・検証を細かい許可待ちで止めない。

# 6. 確立した事実を再利用する

ChatGPTからCodex、または別セッションへ渡すときは、単なる「やること」だけでなく次を含める。

```text
【変更ティア】 T0 / T1 / T2 / T3
【対象】 repo / branch / commit
【確立した事実】 根本原因 + 根拠 / 関係ファイル地図 / 確認済み事項
【棄却した仮説】 何を確認し、なぜ違うと判断したか
【greenベースライン】 CIまたはテスト結果 + SHA + 日付
【Codexで回すテスト】 targeted / regression の具体的な対象
【Codexで回さないもの】 再調査、不要なfull suite、通常EXEビルド等
【残作業】 Windows・実機・ローカル依存で未確認のものだけ
```

引き継ぎの `【確立した事実】` が現在HEADでも有効なら、Codexはそれを再導出しない。再調査は、その事実を無効にする差分・矛盾・新しい失敗が見つかった場合だけ行う。

長期化するT2/T3だけ、必要に応じて `INVESTIGATION.md` 等へ根本原因、ファイル地図、棄却仮説、greenベースラインを固定する。T0/T1のために毎回新しい台帳ファイルを作る必要はない。

# 7. 常に残す安全弁

ティアやクレジット節約に関係なく、次は省略しない。

- 正式repo、branch、HEAD SHA、dirty tree確認
- 変更ティアと短いblast-radius宣言
- 変更箇所に対するtargeted test
- T2以上の金額・在庫・印刷・共有フォルダ・HDD・DB等のデータ保全確認
- build / deploy / release変更のdry-runまたは非破壊確認
- commit / push前のdiffレビュー
- 秘密情報・実運用データ・不要生成物の混入確認
- 実機未確認事項を「確認済み」としない
- 本番反映、タグ、merge等の既存承認ルール

# 8. Codexで特に禁止する無駄

- 小変更で管理文書一式とrepo全体を毎回読み直す
- 既存機構の局所修正で他repoを横断調査する
- targeted failureの診断でfull suiteを回す
- コード変更なしで同じfull suiteを複数回回す
- passログ全文を会話へ貼る
- ChatGPT側ですでに確立した根本原因を最初から再調査する
- 通常のEXEビルド、通常の配布更新をCodex作業に戻す

# 9. CI整備との関係

フルスイートをCIへ寄せられるrepoでは、push時CIを正式な回帰ゲートにする。

CIが未整備または対象テストをカバーしていないrepoは、その事実を明示する。CI未整備を理由にCodexの各修正ループでfull suiteを繰り返してはならない。

今後CIを追加するときは、既存の安定したworkflowを再利用し、agentのローカルfull test依存を段階的に減らす。

# 10. 完了時の報告

作業完了時は、長い実行ログではなく次だけを簡潔に報告する。

- 変更ティア
- 変更した内容
- targeted / regression結果
- CI結果またはCIで確認予定の範囲
- 実機で未確認のもの
- commit SHA / branch
- 本番反映の有無

このポリシーの目的は、品質を落とすことではない。**事故防止に効く確認を残し、安心感だけの重複確認を削ること**である。
