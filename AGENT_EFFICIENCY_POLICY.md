# Agent Efficiency Policy — Legacy Reference

この文書は、旧T0〜T3運用・読み込み予算・CI必須ゲート等の履歴を残していた旧ポリシーです。

**現在のDevelopment運用では必読・正本ではありません。**

現行ルールは [OPERATING_CONTRACT.md](OPERATING_CONTRACT.md) の A / B 二経路を正とします。

## 現在も有効な一般原則

- 依頼と変更に必要なコード・仕様だけを読む。
- 変更箇所に対応するtargeted testを優先する。
- 同じ対象・同じ条件の成功済みテストを理由なく重複実行しない。
- 長い成功ログを再読せず、失敗箇所と必要な要約を扱う。
- 本番データ・秘密情報・既存変更を保護する。
- 実行していない検証を確認済みと扱わない。
- 定型処理は既存の安全なスクリプトを利用する。

## 廃止した必須ルール

以下は現在の必須運用ではありません。

- T0 / T1 / T2 / T3 の分類・宣言
- ティア別の文書読み込み予算
- ティア別の固定テスト回数・反復上限
- candidate SHAのGitHub Actions green必須化
- CI coverage表を毎開発判断の正本として使うこと
- 毎ターンの契約・工程再照合
- 固定フォーマットの完了報告・実機AI引き継ぎ

必要な検証は、変更内容・既知リスク・対象環境に応じて選びます。

このファイルを再び詳細なガバナンスポリシーへ拡張しないでください。効率化は文章量ではなく、**不要な再調査・重複テスト・外部サービス待ちを減らせたか**で評価します。
