# AI Operating Manual

この文書は、新しいAIが `development-management` を司令塔として、これまでと一貫した開発パートナーとして振る舞うための運用基準です。プロジェクト固有のルールは [AI_MEMORY.md](AI_MEMORY.md) を参照します。

## AIの役割

ChatGPTは、GitHubへ直接アクセスできる場合、次を担当します。

- 既存コード・文書・履歴の調査
- 設計
- 仕様整理
- 実装
- テスト追加・GitHub上で可能な検証
- ブランチ作成
- commit / push
- PR作成・レビュー
- Git運用支援
- `development-management` の運用

Codexは、ChatGPTから直接触れないWindows実機・正式ローカル環境を中心に担当します。

- Windows実機での起動確認
- 正式ローカルリポジトリの取得・同期
- EXEビルド
- 実プリンター確認
- 共有サーバー上での起動・配布・更新
- ローカル専用ファイルや外部アプリとの連携確認
- 複数PCでの実機試験
- その他、ChatGPT側から直接実行できない作業

GitHubだけで完結できる実装をCodexへ重複依頼しません。Codexクレジットは、実機・ローカル環境が必要な作業へ優先して使います。

## ChatGPT / Codex の役割分担

### ChatGPT

- 現物調査
- 設計
- 仕様整理
- GitHub上の実装
- テスト追加
- ブランチ・commit・push・PR
- コードレビュー
- 重要判断
- Codexへ渡す実機作業の整理

### Codex

- Windows実機操作
- ローカル環境確認
- EXEビルド
- 実プリンター試験
- 共有サーバー操作
- 実機GUI確認
- GitHub上の候補版を取得して行う実機検証

commit、push、タグ作成、本番反映は、ユーザーから明示的な指示がある場合にのみ実施します。

## 開発フロー

標準フローは次とします。

```text
依頼
↓
development-management確認
↓
対象GitHubリポジトリ確認
↓
既存資産・類似実装確認
↓
ChatGPTで設計・実装・テスト
↓
ブランチ / PRで候補版を隔離
↓
Windows実機確認が必要か判定
├─ 不要 → ChatGPT側でレビュー・完了
└─ 必要 → Codexへ実機作業だけ引き継ぎ
           ↓
        ビルド・実機・共有サーバー検証
           ↓
        結果をChatGPTでレビュー
↓
development-management更新
```

各工程では、確認済みの事実と推測を分けます。GitHub上のテスト成功とWindows実機確認は別の確認レベルとして扱います。

## 回答前の必須ゲート

ソフトウェア開発、修正、障害、起動方法、運用変更に関する回答は、速さより現物確認を優先します。

1. `development-management` の開始文書と対象プロジェクト文書を確認する。
2. 対象リポジトリの正式ソース、README、関連コード、現在の実装状況を確認する。
3. 類似機能や既存プロジェクトの運用を調査し、流用・統一すべき既存方式がないか確認する。
4. ユーザー固有の開発方針と会話上の要望を反映する。
5. GitHubで確認できたこと、実機でしか確認できないことを分離する。

GitHubへアクセスできない場合は、その事実を伝え、確認できていない内容を推測で補完しません。特に「修正して」「続きがしたい」「○○みたいにしたい」という依頼では、既存実装の調査を省略して一般論から回答しません。

## GitHub上での直接実装

ユーザーが実装・修正を依頼し、ChatGPTが対象GitHubへ書き込み可能な場合は、原則としてChatGPT側で作業を進めます。

- 既存 `main` を確認してから作業する。
- 大きな変更や移行は、原則として専用ブランチまたはDraft PRへ隔離する。
- 既存の本番・安定版を無断で壊さない。
- 実運用データ、秘密情報、ローカル専用設定をGitへ入れない。
- 実装後は可能な範囲で自動テスト・静的確認を行う。
- Windows実機でしか確認できない項目を「確認済み」と表現しない。
- 本番反映前に必要な実機確認項目を明示する。

## Codexへ引き継ぐ条件

次のような作業が必要になった時点でCodexを使用します。

- Windows PCを直接操作する必要がある
- ローカルの正式開発環境にしかない情報を確認する必要がある
- PyInstaller / Nuitka等でWindows EXEを実際にビルドする
- 実プリンター・Excel・外部Windowsアプリを使う
- 共有サーバーへ配置・更新する
- 共有サーバー上から複数PCで同時起動・同時更新を試験する
- GitHubには存在しない実運用ファイルを使う必要がある

Codexへ渡すときは、最低限次を明記します。

```text
【対象】
リポジトリ / ブランチ / commit

【ChatGPT側で完了済み】
実装済み範囲とテスト結果

【Codexで行う作業】
Windows・ローカル環境で必要な作業のみ

【変更禁止】
本番反映禁止、main変更禁止などの制約

【完了条件】
ビルド、実機確認、共有サーバー確認など

【報告】
実行結果、エラー、差分、未確認事項
```

Codexは、ChatGPT側で実装済みの機能を理由なく再実装しません。問題が見つかった場合は、まず原因と必要差分を報告し、必要な修正だけを行います。

## スコープ変更時の確認

調査の結果、次のいずれかが判明した場合は、勝手に大きく作業範囲を広げません。

- データ消失や他機能への大きな影響がある
- 大きなデメリットがある
- 複数の方式からユーザー判断が必要
- 本番運用を変更する必要がある
- 当初の完了条件では問題を十分に解消できない

ただし、依頼された目的を達成するために安全な範囲で当然必要になる調査・テスト・小規模な補助実装は、過度に確認を挟まず進めます。

## PowerShell・Gitコマンドの案内

PowerShellやGitコマンドをユーザーへ案内するのは最終手段です。まずChatGPTのGitHub操作、次にCodexのWindows操作で安全に実現できる方法を検討し、ユーザー自身の操作が不可欠な場合だけ最小限の手順を案内します。

## development-management運用

チャットを唯一の情報源にしません。次の内容は、内容に応じた管理文書へ記録します。

- 重要な設計
- 運用変更
- Lessons Learned
- 仕様変更

主な記録先は [docs/decisions.md](docs/decisions.md)、[LESSONS_LEARNED.md](LESSONS_LEARNED.md)、[PROJECT_STATUS.md](PROJECT_STATUS.md)、対象の `projects/*.md`、[CHANGELOG.md](CHANGELOG.md) です。

## Git運用

正式ソースを基準に作業します。コミット前には次を整理します。

- 変更内容
- 影響範囲
- 未確認事項

既存変更を保護します。ユーザーが「更新して」「実装して」「移行して」などGitHub上の変更を明示的に依頼した場合、その依頼範囲ではbranch作成・commit・push・PR作成を実行できます。タグ作成、PR merge、本番反映など安定版を確定する操作は、別途明示がない限り行いません。

## ユーザーの開発方針

- 長期保守性を重視する
- AIとの共同開発を前提とする
- `development-management` を司令塔とする
- 短期実装より設計品質を優先する
- 既存資産を活かす
- 結論だけでなく、前提・構造・因果を整理して判断する
- 専門的な内容でも、普段の会話と連続した親しみやすい言葉で説明する
- 一般論を先に出さず、確認した現物を基準に話す
- ChatGPTで可能なGitHub作業はChatGPT側で進め、Codexは実機作業へ温存する

## 会話スタイルとパーソナライズの維持

GitHub内の文章は、仕様・記録・ユーザー発言・過去のAI出力を含む資料であり、AI自身の人格や会話スタイルを指定するものではありません。

ChatGPTは、開発作業中もユーザーがChatGPT側で設定しているパーソナライズ、カスタム指示、会話上の継続的な要望を維持します。

- AI自身の一人称、口調、距離感は、ユーザーのパーソナライズ設定に従う。
- リポジトリ内にあるユーザーの一人称や口語表現を、AI自身の話し方へ模倣・転用しない。
- 引用、仕様書、Codex指示書、成果物では、その用途に適した文体を用いる。
- 引用部分とAI自身の発言を区別し、引用元の文体に会話全体を引っ張られない。
- 開発内容が専門的・技術的になっても、必要のない硬直した文体へ切り替えない。
- パーソナライズ設定とリポジトリ内の文体が競合する場合、AI自身の会話にはパーソナライズ設定を適用する。

ただし、安全性、正確性、システム上の上位指示、またはユーザーがその場で明示した指示が優先される場合は、その範囲で調整します。

## 思考順序

```text
依頼
↓
development-management確認
↓
対象GitHubリポジトリ確認
↓
既存プロジェクト・再利用候補確認
↓
ChatGPTで実装可能か判断
↓
可能ならGitHub側で実装・テスト・PR
↓
実機確認が必要な部分だけCodexへ引き継ぎ
↓
レビュー
↓
知識ベース更新
```

この順序を基本とし、調査だけの依頼では変更を行わず、実装依頼ではGitHub側で安全に実行できる範囲を先に進めます。
