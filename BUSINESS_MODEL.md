# Business Model

## 文書の目的

この文書は、`development-management` が管理する業務効率化システム全体の共通設計思想を定義します。各プロジェクト固有の仕様書ではなく、すべてのシステムが共通して参照する業務モデルです。

新しいシステムを設計するときは、最初に本書の業務モデルへ当てはめ、その上でプロジェクト固有の仕様を設計します。本書は完成版ではなく、新しいシステムの開発を通じて継続的に育てます。

## 業務とは何か

本プロジェクトでは、業務を「タスクの集合」として扱います。タスクとは、人またはシステムが実施・管理する単位であり、業務内容が異なっても共通の構造で表現できるものとします。

例:

- 締め作業
- 発注
- 清掃
- 点検
- 棚卸し
- 衛生管理
- 季節準備

## タスクモデル

すべてのタスクは、次の要素を持ちます。

### 1. 発生条件（Trigger）

タスクがいつ発生するかを定義します。

例: 月末、月初、日付範囲、曜日、予約人数、使用会場、在庫不足、手動実行

### 2. 対象（Target）

誰、または何を対象とするかを定義します。

例: Aシフト、Bシフト、清掃担当、発注担当、システム、特定設備

### 3. 負荷（Load）

タスクが担当者へ与える業務量です。可能な限り数値化し、割り振りや最適化へ利用します。

例: 持ち点、作業時間、優先度、推定工数

### 4. 制約（Constraint）

タスクを実施できる条件を定義します。

例: 特定シフトのみ、保有資格、営業時間外、担当者限定、同時実施不可

### 5. 状態（State）

タスクの現在の進行状況です。基本状態は次のとおりです。

- 未発生
- 未着手
- 割り振り済み
- 進行中
- 完了

必要に応じて、システム固有の状態を追加できます。

### 6. 完了条件（Completion）

何をもってタスクを終了とするかを必ず明確に定義します。

例: 完了ボタン、自動判定、承認、システム更新、作業記録

## タスクライフサイクル

すべてのタスクは、次の流れを基本とします。

```text
発生条件成立
        ↓
タスク生成
        ↓
割り振り
        ↓
実行
        ↓
状態更新
        ↓
完了
```

システムごとの差異があっても、この流れを基本モデルとします。

## 共通ルール

### 状態管理

タスク定義と発生状態を分離します。

タスク定義:

- 何をするか
- 誰が担当できるか
- 負荷
- 制約
- 発生条件

発生インスタンス:

- いつ発生したか
- 誰へ割り振られたか
- 現在の状態
- 完了日時

### 負荷管理

負荷は、担当者間の公平性を保つための指標として利用します。算出方法はシステムごとに異なってもよいものとしますが、割り振り判断に利用できる数値で管理することを推奨します。

### 制約管理

制約は、担当者や設備の安全性と運用ルールを守る条件であり、負荷より優先します。

### 自動化の原則

システムは、可能な限り次を自動化します。

- 発生
- 割り振り
- 状態管理

ただし、業務判断が必要な部分では、人が介入できる設計を維持します。

## 共通アーキテクチャ

業務効率化システムは、次の概念を共通基盤として設計します。

```text
業務
│
├ イベント
│   └ 月末・予約・在庫不足など
│
├ タスク
│   └ 実施すべき作業
│
├ リソース
│   └ 人・設備・備品・会場
│
├ ルール
│   └ 制約・優先順位・負荷
│
└ 状態
    └ 未着手・進行中・完了
```

新しいシステムは、このモデルへ適用できるかを確認してから設計します。

## システムへの適用例

### next-day-setup

- 発生条件: 月末、月初、日付範囲など
- 負荷: 持ち点
- 対象: シフト、担当者
- 制約: シフト、残りポイント
- 状態: 未割り当て、割り当て済み、完了

### 発注システム

- 発生条件: 在庫不足
- 負荷: 発注件数、作業量
- 対象: 発注担当
- 制約: 在庫、仕入条件
- 状態: 未発注、発注済、納品済

### 浴場衛生管理

- 発生条件: 曜日、経過日数、測定結果
- 負荷: 清掃、点検作業量
- 対象: 担当者
- 制約: 営業時間、設備状態
- 状態: 未実施、実施済、確認済

### 将来追加するシステム

設備管理、清掃管理、その他の業務効率化システムも、同じタスクモデルへ適用できるかを最初に確認します。適用できない要件が見つかった場合は、個別ロジックを追加する前に、共通モデルを拡張すべきか検討します。

## 設計原則

新しい業務が発生しても、専用ロジックを増やすことを第一選択にしません。まず既存の業務モデルへ適用できるかを検討し、共通化できる場合は共通基盤へ実装します。

各システムは個別最適だけを目指さず、共通モデルを共有することで、保守性、拡張性、AIとの共同開発効率を高めます。ただし、共通化そのものを目的にせず、安全性や実際の業務要件を優先し、プロジェクト固有の差異は明示的に定義します。
