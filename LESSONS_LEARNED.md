# 教訓

ここには再発防止のための知見のみを記録する。
不具合だけでなく、運用改善・設計改善・AIとの開発で得られた教訓も記録対象とする。

- `robocopy /E` ではコピー先に古い `_internal` が残る。ランタイム領域は完全同期が必要。
- Python 3.12と3.13のランタイムが共有版に混在すると、開発環境では動いても共有版だけ起動に失敗することがある。
- 正式ソース以外で開発すると、変更・Git履歴・進行状況が分散し、どれが正しいか判断できなくなる。
- 未コミット変更はGitHubから確認できない。引き継ぎ時はローカルのGit状態を必ず確認する。
- Windows PowerShell 5.1で実行するスクリプトは、互換構文と文字コードを意識する。文字化けや解析エラーを起動前に確認する。
- 実行時データとソース、配布物を分離する。共有版更新で業務データを削除・上書きしない。
- `master_settings.json` がない環境ではビルドが失敗し得る。実値をGit管理せず、ビルド時の扱いとサンプル設定を明確にする。
- 「最新タグ」「未コミットの最新状態」「実運用中の版」は別物として記録する。
- Codexのサンドボックスでは正式ソース外への書き込みや一部Git操作が制限される。権限エラー時に別フォルダへ回避コピーせず、対象と権限を確認する。
- 対象プロジェクト内にビルド手順がない場合でも、既存プロジェクトに実運用済みの方式がある可能性がある。新しいビルドツールを順番に試す前に、NDS等の類似Windowsアプリを横断確認する。
- 「既存資産を活かす」はコードだけでなく、ビルド、配布、認証、同期、CSV取得、印刷、DB移行、UI回帰テスト、Windows運用まで含む。再利用可能な入口は `REUSE_MAP.md` に集約する。
- 新しい方式の確立に大きな調査コストがかかった場合、その成功条件をそのプロジェクトだけに閉じ込めない。次回の探索コストを下げるため、横断再利用できる条件・比較対象・失敗方式も記録する。
- Windows の `.cmd` は3点を守る。(1) ASCII のみ。`cmd.exe` は CP932 環境で非 ASCII のコメント行を実行しようとする。(2) CRLF 改行（`.gitattributes` の `*.cmd eol=crlf` で担保）。(3) `if (...)` ブロック内の `echo` に `(` `)` を書かない。`)` がブロックを早期に閉じ `... was unexpected at this time.` になる。`python -c "..."` の複数 import は `,` でなく `;` で区切る（先頭が引用符付きパスだと cmd がカンマで誤分割）。動作確認は必ず `cmd.exe /c`（実ダブルクリック相当）で行う。
- `python -m venv` の ensurepip は深いパスで失敗する（MAX_PATH 260）。CI・検証で一時 venv を作るときは短いパスに置く。

<!-- 旧cloneから救出 -->
- 業務分類では、実在確認のない特殊ケースを弱い情報から補完しない。利用区分のような正式な分類項目を一次判定にし、補完条件には実データ、ユーザー確認、テストの根拠を残す。
