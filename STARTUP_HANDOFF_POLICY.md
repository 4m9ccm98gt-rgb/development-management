# Startup Handoff Policy

この文書は、Windowsローカル開発環境の**初回準備が必要な場合だけ**参照する補助資料です。

正本は [OPERATING_CONTRACT.md](OPERATING_CONTRACT.md) です。本書は単一の開発ルート（DCCのAI依頼欄）を上書きせず、別の開発開始経路も作りません。

## 1. 基本方針

初回準備の目的は、今回必要な開発・確認を安全に始められる状態へすることです。

- ローカルrepo、remote、branch、runtime、依存関係、必要なローカル設定等を確認する。
- `RUN_DEV`、BUILD、UPDATE等に既存の安全な入口がある場合は利用する。
- ユーザーへ長いGit / PowerShell手順を覚えさせるより、既存のワンクリック入口を優先する。
- 今回不要な将来工程まで一括監査することを必須にしない。

## 2. 初回準備の進め方

実装・設定変更が必要な場合は、DCCのAI依頼欄から開始します。準備自体は既存の安全なユーザー操作（ワンクリック入口）で進めます。

- 指定範囲を超えてcandidate確認、BUILD、deploy等へ自動進行しない。
- 本番データ、秘密情報、Git管理外業務データを保護する。

## 3. 最低限の開始条件

今回の作業に必要な範囲で、次を確認します。

- 正式repo / branchが特定できる。
- 既存のtracked変更を勝手に破棄しない。
- 必要なruntime / dependencyが利用できる。
- 今回必要な起動・検証入口が利用できる、または不足が明示されている。
- 秘密情報や実運用データをGitへ入れない。

不足が恒久的な開発入口そのものなら、DCCのAI依頼として追加します。

## 4. やらないこと

- `reset --hard`、force push、無断rebase等で既存履歴を壊す。
- tracked変更や業務データを勝手に破棄する。
- 全repoで③〜⑤相当の入口を毎回一括監査する。
- 初回準備完了のためだけに不要なBUILD / deploy / 実機確認を実行する。
- 初回準備を独立した必須フェーズとして毎開発で再判定する。

初回準備が済んだら、通常のAI依頼経路へ戻ります。
