# Startup Handoff Policy

この文書は、開発対象の**初回スタートアップ作業**を誰に渡すか、どこで止めるかを定義する正本ポリシーです。

`OPERATING_CONTRACT.md` の通常工程・停止条件を変更するものではありません。通常工程①〜⑤へ入る前の前提整備だけを扱います。

## 1. 基本方針

開発対象のスタートアップが未完了で、通常工程へ入るために複数のGit操作、branch準備、手動ファイル配置、依存関係やローカル環境の初期設定などが必要な場合、ユーザーへその手順を覚えさせません。

その作業は **「⓪ スタートアップ」** として Codex / Claude 等の実機AIへ渡します。

これは③〜⑤の通常代行ではなく、通常工程を安全に開始できる状態へ整えるための前提作業です。

**スタートアップは「現在の次工程だけ開始できる」状態では完了としません。対象repoの通常運用で使用する正式ワンクリック入口を一式監査し、必要な入口がすべて利用可能、または明示的に対象外と確認できた時点で完了とします。**

## 2. スタートアップ未完了の判定

次のいずれかに該当する場合は、スタートアップ未完了として扱います。

- 正式ローカルrepoが無い、または対象repoとして安全に使える状態か未確認
- 必要なremote / expected branch / worktreeの準備が済んでいない
- candidate同期・起動・build・deploy / update等に必要な正式入口や補助スクリプトがローカルにまだ無い
- 通常工程へ入る前に手動ファイル配置やbranch切替が必要
- runtime / dependency / virtual environment / ローカル設定等の初回準備が必要
- ユーザーへ複数のGitコマンド、PowerShell操作、ファイル配置、環境初期設定を手順として要求する必要がある
- 対象repoで使用するはずの正式ワンクリック入口がrepo / candidate側に未整備
- ③は開始できるが④の `RUN_DEV` が無い、または④は開始できるが⑤のbuild / update入口が無い等、後続工程で再度初回準備が必要になる状態

単純なワンクリック操作や通常GUI操作だけで、対象repoの通常運用を継続できる状態ならスタートアップ済みとして扱えます。

## 3. 担当

スタートアップ未完了と判定した場合、ChatGPTはユーザーへ長い初回手順を列挙せず、Codex / Claude 等の実機AIへスタートアップ作業として引き継ぎます。

- ユーザーが特定エージェントを指定している場合は、その指定を維持する。
- Claude指定をCodexへ、Codex指定をClaudeへ、または指定実機AIをChatGPTへ勝手に置き換えない。
- 特定指定がない場合は、Codex / Claude 等の実機AIへ渡せる完成済みの共通スタートアップ指示文を作る。
- 現在のチャットから指定実機AIを直接呼べない場合は、ChatGPTが代行せず、完成済みhandoff指示文を返して停止する。

このスタートアップ委任は `OPERATING_CONTRACT.md` §2.1 の User Override とは別です。ユーザーが実機AI利用を明示していなくても、スタートアップ未完了という条件で発火します。

## 4. スタートアップの範囲

スタートアップの目的は、**対象repoの通常運用で必要な初回準備と正式入口の配置・利用可能性確認を一度に完了させること**です。

通常工程そのものは実行しません。

### 4.1 ライフサイクル入口のコンプリート監査

Windowsアプリでは、スタートアップ完了前に少なくとも次を確認します。対象repoで不要な工程は `N/A` と明示します。

| 工程 | 標準入口 | スタートアップ完了条件 |
|---|---|---|
| ③ candidate同期 | `SYNC_CLICK_ME.cmd` | 通常同期入口、または初回だけ必要な安全なbootstrap入口が利用可能 |
| ④ 実機確認 | `RUN_DEV.cmd` 等 | ユーザーがコマンド手打ちなしで開発版を起動できる |
| ⑤ build | `BUILD_*_CLICK_ME.cmd` 等 | build対象repoなら正式ワンクリック入口が利用可能 |
| ⑤ deploy / update | `UPDATE_*` / `UPDATE_SHARED_FOLDER.cmd` 等 | 配布更新対象repoなら正式ワンクリック入口が利用可能 |

ファイル名はrepo固有の正式名を許可しますが、READMEや運用文書だけにCLI手順があり、ユーザーがコマンドを手打ちしないと進めない状態は「正式入口あり」と判定しません。

**⓪完了報告の前に、③〜⑤の各入口について `READY / N/A / MISSING` を必ず列挙します。1つでも `MISSING` があれば⓪は未完了です。**

### 4.2 正式入口がrepo / candidate側に無い場合

恒久的に必要な `RUN_DEV.cmd`、build、update等の正式入口がGitHub側に存在しない場合、ローカルrepoへuntrackedの代用品を置いてスタートアップ完了扱いにしません。

その不足は**開発側の未整備**として扱い、ChatGPTへ戻して②開発・作成で正式なtracked入口を実装し、必要なテスト / CIを通してcandidateを更新します。その後、⓪スタートアップを再開してローカル利用可能性を確認します。

これにより、実機だけに存在する非公式スクリプトや、将来candidateと衝突する先置きファイルを標準運用にしません。

例外は、**candidate自身が初回同期後に正式tracked入口を導入するため、そのcandidateへ到達する前だけ入口が存在できないケース**です。この場合に限り、repo外の一時bootstrap入口を使用できます。

bootstrapは以下を満たします。

- repo内部へcandidateと衝突するuntrackedファイルを先置きしない
- 正式repo / remote / branch / SHA / dirty状態をfail-closeで検証する
- 通常工程の安全条件を弱めない
- candidate到達後はrepo内の正式tracked入口へ切り替える
- 一時bootstrapを恒久的な正式入口として扱わない

### 4.3 ③ candidate同期の前提整備

必要に応じて以下を行い、③を開始できる状態を作ります。

- 正式ローカルrepo確認
- remote / repo identity確認
- expected branch準備
- 正式 `SYNC_CLICK_ME.cmd` の利用可能性確認
- candidate導入前で正式入口を配置できない場合のrepo外bootstrap準備
- 同期入口を安全に実行できる前提確認

candidate同期そのものは⓪に含めません。

### 4.4 ④ 実機確認の前提整備

- 必要なruntime / dependency / virtual environmentを準備
- 正式 `RUN_DEV.cmd` 等のワンクリック起動入口が利用可能か確認
- 必要なローカル設定を、秘密情報・業務データ保護ルールを守って準備

`RUN_DEV.cmd` 等が恒久的に必要なのにrepo / candidateに存在しない場合は §4.2 に従い、⓪を完了させず②へ戻します。

アプリ起動後のGUI・機能・実紙確認は④として別に扱います。

### 4.5 ⑤ build / deploy / updateの前提整備

- build対象なら正式 `BUILD_*_CLICK_ME.cmd` 等の存在・利用可能性を確認
- deploy / update対象なら正式 `UPDATE_*` 等の存在・利用可能性を確認
- 配布先や必要な前提設定の有無を確認

恒久入口が不足していれば §4.2 に従い、⓪を完了させず②へ戻します。

本番build、deploy、update、本番反映そのものは⑤として別に扱います。

## 5. 安全条件

スタートアップを理由に、次工程へ自動進行しません。

- candidate同期
- GUI / 機能確認
- 実紙 / printer確認
- full regression
- build
- deploy / update
- 本番反映

また、以下を禁止します。

- `reset --hard`、force、履歴破壊、無断rebase
- 自動stashやtracked変更の勝手な破棄
- 秘密情報のGit管理
- 実運用データ、Git管理外業務データ、ローカル設定の無断削除・上書き・移動
- スタートアップ範囲を理由なく別工程・別作業へ拡張すること
- repoに存在しない恒久入口をuntrackedで先置きし、正式入口として扱うこと
- 1つ以上のライフサイクル入口が `MISSING` のまま⓪完了を報告すること

## 6. 完了条件

スタートアップ完了時は、最低限以下を報告します。

```text
【工程】⓪ スタートアップ
【対象】repo / local path / branch
【実施内容】初回準備した内容
【入口監査】
- ③ SYNC: READY / N/A
- ④ RUN_DEV: READY / N/A
- ⑤ BUILD: READY / N/A
- ⑤ UPDATE / DEPLOY: READY / N/A
【現在状態】次に開始できる正式工程
【未実施】通常工程そのものは未実施
【停止】ここでスタートアップ完了として停止
```

`MISSING` が1つでもある場合は完了報告を出さず、何がGitHub側の未整備かを明示してChatGPTへ戻します。

スタートアップ完了後、ChatGPTは `OPERATING_CONTRACT.md` に戻り、通常の①〜⑤の現在工程と担当を再判定します。

## 7. ChatGPTの継続ゲート

開発作業で次の実機操作を案内する前に、以下を確認します。

> この開発はスタートアップ済みか。③SYNCだけでなく、④RUN_DEV、⑤BUILD、⑤UPDATE / DEPLOYまで対象repoで必要な正式ワンクリック入口がすべてREADYまたはN/Aか。後続工程で初回準備が再発する状態なら⓪は未完了。恒久入口がGitHub側に無ければローカル代用品で済ませず②へ戻して正式実装し、その後⓪を再開する。ユーザーへGit / PowerShell / CLI手順を覚えさせない。

この確認は `OPERATING_CONTRACT.md` の工程担当チェックと併用します。
